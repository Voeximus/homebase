# Homebase — Session Handoff / Current State

_Last updated: 2026-06-20k (anomaly-ack, smarter adherence, saved meals, meal-UI). This is the regrounding doc for resuming work on Homebase. Read this first, then `C:\Users\ginoc\.claude\projects\C--\memory\project_homebase.md` for the deep history._

> **⭐ Homebase now has a LIVE Plaid bank feed.** Gino's real Bank of America accounts auto-sync (balances + categorized transactions). The old manual/reconciled data was wiped (full backup taken); **Plaid is now the source of truth.** See **"⭐ Live bank feed"** below. The pre-Plaid baseline is kept only for historical reference.
>
> **⚠️ This repo is PUBLIC — never commit credentials, secrets, or the financial-data backup.**

> **⭐⭐ 2026-06-20k — 5 QoL FIXES: anomaly-ack, smarter adherence, saved meals, meal-UI (shipped live, commit `81870cd`; deploy green; `schema_v20` applied).**
> - **Today's Meals open/closed persists** (`localStorage hb-meals-open`) — switching Health↔Finance and back no longer re-expands it (`SoloMode` useState init from LS + effect to write).
> - **Unusual Purchase is now actionable** — the Home tile opens a dedicated **`AnomalySheet`** (`src/views/redesign/AnomalySheet.tsx`) listing each flagged charge with the reason ("{ratio}× your usual {cat}"); per-row **Recategorize** (→ TxnSheet) or **dismiss**. Dismiss sets **`transactions.anomaly_ack`** (schema_v20) via `acknowledgeAnomaly` → `buildVMs` anomaly detection excludes it → never re-flags (synced both phones). The old "→ all-activity → category screen, flag stays" flow is gone (onAnomaly no longer opens the ledger).
> - **Adherence is honest** (`src/lib/adherence.ts`): a day is "logged"/adhered (streak + compliance credit) only when **kcal ≥ 70% of target** (`ADHERE_PCT`); a logged-but-light day is the new **"partial"** status (blue `#4f7ab0`, in the legend) — no streak/compliance credit. Today's partial is forgiven (mid-day). `dayStatusOf(day, target?)` + `adherenceStats(..., target)` now take the person's target (`DAILY[person]`). Verified: 1 egg (72/2800) = partial/no-streak; crossing 70% (2282) flips to logged/streak +1.
> - **Saved / favorite meals** — new **`saved_meals`** table (schema_v20, household-shared, RLS + realtime) wired through `HealthStore` (`savedMeals` + `addSavedMeal`/`deleteSavedMeal`, `SavedMeal` type in mealLog). Save a meal via the **Bookmark** on a meal card (solo) or **Save** on the Together dish → `SaveMealSheet` (name prompt, auto-suggests joined item names). Re-add one-tap from the **`SavedMealsBar`** chips (solo → a fresh meal with cloned item ids; together → items dropped into the dish via `foodFromItem`); delete via the chip ×. Verified solo end-to-end.
> - **Macro ring number scales to digit count** (`DaySummary` + `PersonSummary`): 4-digit kcal (2800/1550) was a fixed 30px and overflowed the 96px ring on iPhone → now 26px for 4-digit (34/30/26/22 by length); PersonSummary 18/16/14. Verified 2800 = 26px, 62px wide inside the 76px inner ring.
> - **Note:** #2 is build-verified + logic-traced only — no harness mounts the finance `FinanceTabs`/`AnomalySheet` (needs auth). Worth an on-device check: a flagged buy → tile → dismiss → it's gone and stays gone.
>
> **⭐⭐ 2026-06-20j — CATEGORY MERGE: Health → Household + Hygiene (shipped live, commit `c46b6a1`; deploy green; `schema_v19` applied + verified).** Fixes the split "missing category" + redundant-category confusion.
> - **Root cause:** categories are **code-defined** (`src/lib/seed.ts` `DEFAULT_CATEGORIES`, NOT in the DB — `data.categories` is always that constant). The split `<select>`, the recategorize grid, and AddTransactionSheet all read `data.categories`, so they were already consistent with each other — but the budget (`LEAN_VARIABLE` in `plan.ts`) **labels** `shopping` as "Household + hygiene" while the category was NAMED "Shopping", and a redundant `health` ("Health + grooming") line existed. So the user looked for "Household + Hygiene" in the split and saw "Shopping" → "missing".
> - **Fix:** renamed the `shopping` category → **"Household + Hygiene"** (🧴, #f97316) and **removed the `health` category** from `DEFAULT_CATEGORIES`. Merged the two `LEAN_VARIABLE` lines into one **"Household + Hygiene" $200** (`cats: ["shopping","health"]` — keeps the legacy id so any stray row still counts; total budget unchanged → firepower math untouched). Categorizer (`categorize.ts`): `"Health/Personal"` + the pharmacy/hair keyword rule now map to `shopping`. Fixed two dangling refs: `ImportSheet` `CLARIFY_CATS` dropped "health" (added "pets"); `DesignLab` dev mock merged to one line.
> - **DATA MIGRATION (`schema_v19_category_merge.sql`, applied via Management API + verified 0 remaining):** `transactions` / `recurring` / `merchant_rules` / split-slices `health` → `shopping`. Idempotent.
> - **Consistency now holds:** every budget category id (groceries/transport/dining/shopping/pets/subscriptions/entertainment) is in `data.categories`, so the split is a SUPERSET of budget categories — nothing in the budget is unusable in a split. ⚠️ Verified by build + DB migration + code (no harness mounts the finance `TxnSheet`, so the split UI itself wasn't live-clicked — worth an on-device check that the split list reads "Household + Hygiene").
>
> **⭐⭐ 2026-06-20i — EDITABLE WORKOUT HISTORY + MEAL READOUT/CONTAINER POLISH (shipped live, commit `d5b488a`; deploy green).**
> - **Workout history is now manageable (was permanent/rigid):** each past entry in the SoloWorkout History list has a **per-row delete** (Trash → inline two-step confirm, consistent with weight logs) AND a **per-row edit** (Pencil, or tap the row) → opens a new **`EditWorkoutSheet`** (in `WorkoutSection.tsx`): rename, edit/add/remove exercises + sets (reuses `ExerciseBlock` + `ExerciseSearchSheet`), change a cardio **duration** (new `DurationBlock` + `NumIn`), edit notes, or delete (confirm). Save = `upsertWorkout(draft)` (stays `done`). Week count / PRs / volume / the sticky summary all **recompute from the live list automatically** (they're memos on `allWorkouts`). Verified live: edit a Walk 30→45 min round-trips; delete clears history + drops the week count to "to hit your weekly goal".
> - **Add Food popup macro readout line (Gino's main typography gripe — the "226 kcal · 9P 20C 12F per 100g · 1 taco ≈ 80 g" line):** now bold kcal + **color-coded P/C/F** (rose/sky/gold `MACRO_BRIGHT`) with the **unit equivalence on its own muted line** ("1 bagel ≈ 98 g"). High contrast, clear hierarchy.
> - **"Today's Meals" container — smaller when collapsed:** the collapsed state is now a **slim single-line bar (~46px on mobile)** — icon + "Today's Meals" + inline "{n} meal(s) · {kcal} kcal" + an always-available **"+" add button** + chevron — instead of the taller two-line header + footer button. Expands to the meal cards.
> - ⚠️ **Preview note:** the `?meallab` harness window sometimes reports `window.innerWidth = 15` (collapsed iframe) which makes DOM height reads bogus + screenshots time out — use **`preview_resize` to `mobile` (375)** before measuring layout, and rely on computed-style/DOM reads over screenshots.
>
> **⭐⭐ 2026-06-20h — HEALTH UI REFINEMENTS + SPLIT AUTO-MATH (shipped live, commit `4eeb144`; deploy green).** Screenshot-driven polish.
> - **Meal Builder "Today's Meals" container:** added meals no longer stack loosely — they collect into ONE labeled, **collapsible** `<section>` (header = UtensilsCrossed + "Today's Meals" + "{n} meal(s) · {kcal} kcal" + chevron; `mealsOpen` state, default open). MealCards restyled to a darker inner tile (`#0f141c`) so they read as nested items; "Add a meal" lives in the container footer (always reachable). Verified in `?meallab` (add → header updates, collapse hides cards, day total tracks).
> - **DaySummary neutral bg (#4):** the big "GINO'S DAY" card swapped from the rose `HEALTH_GRADIENT` to the new neutral **`HEALTH_HERO`** (`linear-gradient(155deg,#1c2433,#10151d)`) + a thin rose top accent, so the rose Protein sub-counter no longer blends into a rose background. P/C/F keep their distinct colors. New `catColor` exports: `HEALTH_HERO` (neutral hero surface) alongside `HEALTH`/`HEALTH_GRADIENT` (rose = primary actions/highlights only).
> - **Workouts professional color (#2):** the two big hero cards (`WorkoutSummary` week card + `TogetherWorkout` challenge) → `HEALTH_HERO` neutral + sparing rose accent; week-progress dots → rose. Red now reserved for the primary CTA ("Add a workout") + accents, not filling every card.
> - **Food unit portioning (#3):** root cause — the hand-seeded `SEED_FOODS` basics (Eggs/Potato/Banana/Whey) had NO `unit` and win the search over the unit-tagged bundled foods → grams-only. **Added units to those** + a name-based **`unitFor()`** fallback in `nutrition.ts` (bagel/steak/tomato/banana/apple/orange/potato/egg/slice/package/tortilla/sausage…, guarded by a `NON_COUNTABLE` regex for soup/sauce/oil/ground/noodle/etc.), applied in `PortionView` (`const unit = unitFor(food)`) so custom + scanned foods also offer "by the each". Verified: Eggs → "By the egg / Grams", 3 eggs = 150 g = 215 kcal live.
> - **Add Food popup typography (#5):** the monotone gray "This portion" macro line → a high-contrast **3-column colored readout** (kcal hero + P/C/F columns in `MACRO_BRIGHT` rose/sky/gold on dark chips); brighter per-100g reference line.
> - **Finance split AUTO-MATH (the split usability fix):** `SplitEditor` (in `TxnSheet`) rebuilt — starts from a **full unallocated pool** (empty rows); a prominent live **"Left to allocate"** banner counts down as you type; each `$` input is **auto-clamped** to what's still available so the running total can NEVER exceed the transaction total (remaining never negative); a per-row **`+$X` chip** and the banner one-tap **fill the remainder**, plus an **"Even split"** button; save only when it balances exactly with ≥2 distinct categories. ⚠️ Build-verified + logic-traced but NOT live-clicked (no harness mounts the finance `TxnSheet`) — worth a real on-device split test.
>
> **⭐⭐ 2026-06-20g — BUILD FINALIZATION: weight delete/reset, Health red branding, transaction splitting + DEEP AUDIT & FIXES (shipped live, commits `6b11625` then `4358a02`; both deploys green; `schema_v18` applied).**
> - **Weight log (`CalibrationGauge` + `HealthStore`):** per-entry **delete** (inline two-step confirm) + **"Reset weight history"** (modal confirm) — both recompute the weekly avg / least-squares trend / goal-progress live. New store actions `deleteWeight(p,d)` / `clearWeights(p)`. Verified in `?meallab` (log → delete → "—"; reset → all cleared).
> - **Health red branding (#2):** Health mode now reads **rose `#fb7185` + HeartPulse** to match the Settings mode row. `catColor.ts` adds **`HEALTH` + `HEALTH_GRADIENT`** (rose→pink); `ModeToggle` shows HeartPulse with a red active pill; `HealthView` sub-nav active = red; **`HEALTH_GRADIENT` replaced `BRAND_GRADIENT` (the finance green-cyan) across every `MealBuilder` + `WorkoutSection` hero/CTA**. Verified: DaySummary/workout heroes render `rgb(251,113,133)`.
> - **Workouts button:** **"Start a workout" → "Add a workout"** ("Start" stays for routine start).
> - **Transaction splitting (#5):** allocate one charge across categories. `transactions.splits jsonb` (schema_v18); `TxnSplit` type + `Transaction.splits?`; **`spentByCategory` re-buckets split slices** (grand total unchanged since `variableSpentThisMonth` sums full amount); `setTransactionSplits` action (persists only when ≥2 distinct cats, primary = largest slice); **`SplitEditor`** in `TxnSheet` (sum-must-equal-total tolerance, tap-the-balance-chip to fill remainder, merge same-cat); split summary in the detail sheet + **"Split · N ways"** badge in Activity (`buildVMs` `fateOf`).
> - **⭐ DEEP AUDIT (Workflow, 7 pipelines, find→adversarially-verify; 12 raw → 9 confirmed; Health macros/bowls clean).** Fixed: **(CRITICAL)** deleting a *linked-bill payment* restored the debt in the UI but never the DB — `reverse_money_event` keyed off `applies_to.debtId` which bill rows never stamped → now `applyMoneyEvent` **stamps the resolved debtId** AND `reverse_money_event` (schema_v18, `create or replace`, verified live) **resolves the debt via the recurring's linked card for legacy rows** (manual debts only, mutually exclusive → no double-restore). **(HIGH)** the category drill-in (`CategorySheet`/`envVM` in `FinanceTabs`) was split-blind → made it split-aware (matches the budget bar by construction). **(HIGH)** the mark-sent slip + attack-ladder used RAW firepower while Home/Insights used overspend-adjusted → `debtPlan` now applies the same overspend deduction. **(MEDIUM)** `body_weights` realtime refetch had no dirty-guard → added `wt|` dirty keys + a merge in `reloadWeights` (mirrors meals/workouts) so a just-deleted weigh-in can't resurrect cross-device. **(MEDIUM)** Home "Send X at the debt" showed debt+savings → now `next.toDebt`. **(LOW)** two same-category split rows could pass validation then collapse → split now requires **≥2 distinct categories**.
> - **REPORTED, deliberately NOT changed (known/low):** the **"Bank of America"** Profile label is hardcoded (`buildVMs.ts:381`) — correct for this household, but a general fix needs `bank_connections.institution` plumbed into the store; the `payoffSchedule` 240-payday guard can show a truncated "debt-free by" date in a contrived sustained-overspend-to-a-sliver case (unreachable on real firepower); the 8 PM nudge gate is captured at render with no clock-tick re-render, so it can lag the wall clock until the next interaction (self-heals).
> - **Still PARKED:** routine generation + weekly planning (#1, Gino's call — the 4-day Upper/Lower A/B split stays the active structure). **Still pending:** finance-mode typography sweep; wiring the macro target to the calibration gauge (still static `DAILY`).
>
> **⭐⭐ 2026-06-20f — MACRO-PLAN ADHERENCE TRACKING + 8 PM IN-APP NUDGE (shipped live, commit `377085d`; deploy green; schema_v17 applied).** Feature #4, **in-app** (no web-push backend, per Gino's call).
> - **Schema `schema_v17_adherence.sql` (applied):** `meal_days` gained **`status` ('estimated' | 'skipped')** + **`note`**. A day with logged meals is "followed" implicitly; status marks empty days. Wired through `HealthStore` (`mapDay` reads, `writeDay` upserts status+note) → syncs across phones.
> - **`src/lib/adherence.ts`:** `dayStatusOf` (precedence **logged > skipped > estimated > none** — real logged food always wins over an earlier nudge answer) + `adherenceStats` (streak with today's unfinished "none" forgiven; a past "none" gap or a "skipped" breaks it; `compliancePct = followed / (followed + missed)`, null when nothing tracked; a 14-day `recent` strip).
> - **The nudge (`NudgeCard` / `EstimateSheet` in `MealBuilder`):** after **8 PM local**, if **nothing's logged today** + no status + not snoozed, a gentle card in the solo Meal Builder: *"Did you follow the meal plan today?"* **Yes** → an optional "roughly what did you eat?" note → marks the day **followed (estimated, ~on target)** + saves the note; **No** → **off-plan (red)**; **Later** → snooze (per person+date in localStorage). Logging real food anytime dismisses it.
> - **`AdherenceCard`** (solo Meal Builder, below the meals / above the calibration gauge): **streak** (🔥), **compliance %** (colored by value), and a **14-day color-coded strip** (green=logged, amber=estimated, red=off-plan; today outlined).
> - **Verified** in `?meallab` (the `>=20` gate was force-toggled for the test then **reverted**): Yes → amber today-dot + "100% on plan, 1-day streak"; No → red today-dot + "0%"; nudge correctly hidden at 6 AM. Bilingual (~16 ZH). 2-lens review (adherence-math / nudge-store) ran post-deploy.
> - **NOTE — the macro estimate for an "estimated" day = the person's target** (followed the plan ≈ hit targets); the description is captured only as a `note`, NOT parsed into per-food macros (a real description→macros parse via a Claude-API edge fn is a possible upgrade). **Routine generation + weekly planning (#1) is still PARKED (Gino's call).** Pending: the finance-mode typography sweep; wiring the macro target to the calibration gauge.
>
> **⭐⭐ 2026-06-20e — MEAL BUILDER: UNITS + GRAMS ENTRY + NATURAL SHARED-DISH BOWL PORTIONING (shipped live, commit `7addf21`; deploy green).**
> - **Flexible measurement (units + grams):** `Food` gained `unit?: FoodUnit {name, grams}`. A unit-classify Workflow (`scripts/fooddata/units/*.json` → `apply-units.mjs`) tagged **296/878 foods** with a natural countable unit (egg/banana/steak/slice/scoop/breast/potato/tomato…) while ground beef / rice / oils / soups correctly stay **gram-only**. `LoggedItem` gained `qty?`+`unit?` (grams stays canonical = `qty × unit.grams`). **`PortionView`** now: a food WITH a unit defaults to **"By the {unit}"** (a DECIMAL qty stepper `QtyRow`, 1/2/3 chips, shows "= N g") + a toggle to **Grams** and back (preserving the amount); no unit → grams only. Macros update live from whichever. Items display via **`amountLabel`** ("3 eggs" / "1.5 potatoes" / "200 g"). Mix freely in one meal.
> - **Together rework — build the dish, split into bowls:** REPLACED the old per-food mine/hers grams with the kitchen-natural flow. Build ONE **shared dish** (add ingredients by unit/gram → live `WHOLE DISH` total). Then **"Split into bowls"**: each person's bowl in rough total grams ("her ≈ 450 g, his ≈ 600 g"); **bowl macros = dish density × bowl grams** (`bowlMacros`, guards `dishGrams ≤ 0`); **"Even split"** = the `bowls===null` state (dishGrams/2 each); editing one bowl freezes BOTH at current effective then updates the one (`setBowl`); a live per-bowl day-impact breakdown; **"Log both bowls"** scales every ingredient by each person's fraction (`scaleItem`) → logs to each day. Trades a little precision for speed, as asked.
> - **Removed (dead):** the dual-portion UI — `DualPad` / `GramPill` / `BreakdownCard` / the `dual`/`onAddDual`/`youName`… search-sheet props.
> - **Verified** in `?meallab`: 3 hard-boiled eggs = 233 kcal (155×1.5), rice gram-only (no toggle), dish (195 kcal/150 g) → bowls 50/50 (98 kcal each) then You 100 g/67% = 130 kcal, each bowl drawing down its own day. Bilingual (~16 ZH). 3-lens review (units-math / bowls / data-wiring) ran post-deploy.
> - **Per Gino (2026-06-20e):** routine-generation + weekly planning is **PARKED** (back-burner); the 8 PM adherence nudge will be **in-app** (a prompt when you open after 8 PM with nothing logged), NOT a web-push backend. The adherence-tracking system (#4) + that nudge are the remaining open build.
>
> **⭐⭐ 2026-06-20d — WEIGHT TRACKING + AUTO-CALIBRATION, QUICK LOG, MACRO/TYPE POLISH (shipped live, commit `184e48a`; deploy green; schema_v16 applied).** Part 1 of a big 5-part request — the 3 contained pieces. (Routine-generation + adherence/notifications are the next build.)
> - **Typography + macro emphasis (#5):** `index.css` — brighter contrast tokens (`--color-bone`/`taupe`/`faint`), `text-rendering`, a **`.stat`** display class (PROPORTIONAL bold + tabular figures, NOT mono — fixes a "cram" Gino flagged where mono + tight negative tracking squeezed the digits and the unit jammed onto the number), plus `.stat-key`/`.h-title` and a gentle **`.bump`** live-delta pulse (the `.pop` scale-from-0 was too much for frequent updates). The **Daily Macro Summary** (`DaySummary` in `MealBuilder`) now has a bigger calorie ring + the macros as **high-contrast colored counters** (rose/sky/gold via `MACRO_BRIGHT`) on a dark inset, units baseline-aligned + spaced ("130 g" not "130g"). `PersonSummary` (Together) matches.
> - **Weight tracking + AUTOMATIC calibration (#3):** `schema_v16_weights.sql` (applied) — `body_weights (person, date, weight)`, RLS + Realtime. **`src/lib/weightLog.ts`** = weekly averages (Monday-start) + a least-squares **lb/week trend** (robust to daily noise). **`CalibrationGauge` rebuilt:** log ONE weight a day → the app auto-computes this week's average + the trend + verdict. **The manual "Current weight / Weeks on plan" inputs are GONE.** Synced via `HealthStore` (new `weights` state + `setWeight`, immediate upsert + Realtime).
> - **Quick Log (#2):** a low-friction **"Quick log — just an activity"** button in Workouts (esp. for Xinyan) → pick a common activity (Walk / Treadmill / Leg press…) or search → **Duration OR sets×reps** → one tap. Creates a `done` one-exercise session that counts toward the week + history (shows "30 min" for time-based via `sessionStat`/`workoutDuration`). `ExerciseEntry` gained an optional `duration` (JSONB — no schema change).
> - **Verified** in `?meallab`: macro spacing, weight→weekly-avg automation (no "weeks on plan"), quick log → week count + "30 min" history. Bilingual (~25 ZH). 3-lens review (weight-math / store-quicklog / typography) came back with **no real bugs.**
> - **NEXT (the two big systems):** **Routine generation + weekly planning** (#1 — periodization-aware; the 4-day Upper/Lower A/B split STAYS the active structure, untouched) and **Adherence tracking + the 8 PM nudge** (#4 — open call: in-app prompt when you open after 8 PM vs a full web-push backend [VAPID + edge cron + installed-PWA on iOS]). An app-wide typography sweep of FINANCE mode is also pending — this pass did the global foundation + the Health surfaces, not every finance component.
>
> **⭐⭐ 2026-06-20c — HEALTH LOGS NOW CLOUD-SYNC (shipped live, commit `acc871b`; deploy green; schema_v15 applied).** The shared-store / Supabase-sync follow-up — fixes the architecture the review flagged AND makes "Log for both" actually reach the other person's phone.
> - **Schema `supabase/schema_v15_health_sync.sql` (applied via Management API):** `meal_days` (one row per `(person, date)`, the day's `meals` JSONB, unique person+date), `workouts` (one row per session, `exercises` JSONB + `done`), `workout_routines` (custom routines). authenticated-full RLS + added to the `supabase_realtime` publication — exactly the `foods` pattern. Document-per-entity (nested meals/items + exercises/sets in JSONB; the app already thinks in documents). **Validated via a service-role JSONB round-trip** (schema accepts the app's exact shapes; person+date upsert works).
> - **`src/store/HealthStore.tsx` = the single source of truth** (`HealthProvider`/`useHealth`; wraps `HealthView` + `?meallab`). Solo and Together read/write the SAME in-memory state → no divergent caches (the review's core fix). Writes are **optimistic + debounced (700ms)**; a Realtime change from the other device refetches but **SKIPS keys with a pending local write** (`dirty` set) so it can't undo an in-progress edit; **pending writes FLUSH on unmount** (a last-second edit before leaving Health mode isn't dropped — a self-caught data-loss edge). **Failed writes RETRY with exponential backoff** (review fix `65340b1`): on a network/offline/RLS error the write re-schedules itself (cap 30s, ~6 attempts) so a dirty key always has a live timer and self-heals — never stuck-local-forever (the review's one high finding). **One-time `hb-health-migrated` migration** lifts existing localStorage logs to the cloud (guarded against double-insert). Reactivity: state is spread into the context value so every write re-renders consumers.
> - **`MealBuilder` + `WorkoutSection`** now read/write through `useHealth()` (getDay/setDay, upsertWorkout/deleteWorkout, addRoutine/deleteRoutine) instead of `loadDay`/`saveDay`/`loadWorkouts`. **Workout + routine ids are `crypto.randomUUID()`** (real DB PKs); nested JSONB ids (meal items, exercise entries, sets) stay `rowId()`. Active workout = `workouts.filter(person).find(!done)`.
> - **Dead now (sweepable):** `loadDay`/`saveDay` (mealLog) + `loadWorkouts`/`saveWorkouts`/`loadRoutines`/`saveRoutines` (workoutLog) are unused exports (the migration reads raw localStorage directly).
> - **Verified:** store-backed meal + workout flows in `?meallab` (optimistic path; anon can't write cloud) + the DB round-trip. **True cross-device sync needs two authed phones — Gino confirms on device.** The multi-tab / second-device last-writer-wins edge the prior review flagged is now CLOSED (single store + Realtime + dirty-skip + unmount-flush).
>
> **⭐⭐ 2026-06-20 — HEALTH RESTRUCTURED: section-owned switches + a real workout tracker (shipped live, commit `7a42440`; deploy green).** Per Gino: pull the global Mine/Household toggle OUT of the Health header and give each SECTION its own switch (the meal-builder pattern); make the meal builder flexible + always-live; turn "Plan" into a workout tracker.
> - **Header:** the `LensToggle` is GONE from `HealthView`, and the header is **non-sticky** now (slim: Finance/Health + lang + logout + the section nav, relabeled **Meal Builder | Workouts**). Each section owns its **Just me / Together** switch. `HealthView` is a slim shell (~95 lines, was ~793) routing to the two section components; it still accepts `lens/onLens` on its props type (so `App`'s call stays valid) but ignores them. **Solo = the phone `owner`; Together = both** (no more cross-person Household lens in Health).
> - **Meal Builder (`MealBuilder.tsx`) — flexible + live:** dropped the fixed **meals/day count** + Breakfast/Lunch/Dinner slots (and `nextMealAllowance`/`mealsAhead`/`plannedMeals` from `mealLog.ts`). Meals are created dynamically, shown **by position** (Meal 1, 2 …, renumber on delete). A **sticky, always-visible Daily Macro Summary** (`DaySummary`): an animated calorie **`Ring`** (SVG, eased stroke-dashoffset) + P/C/F bars that ease on change, the number **pops** on every update (`.pop` via key-churn = the delta feedback). Together = a sticky **dual** summary (`PersonSummary`, per-person rings). The **calibration gauge moved here** → extracted to **`src/views/CalibrationGauge.tsx`** (it calibrates the macro budget). `STICKY_TOP = calc(env(safe-area-inset-top)+6px)` — if the summary sits oddly under the island on device, that constant is the one-line tweak.
> - **Workouts (`src/views/WorkoutSection.tsx` + `src/lib/workoutLog.ts`):** **Just me** — log a session (exercises → sets of weight × reps, live volume + Epley **e1RM**), build/save/**start** routines, auto **PRs** (best e1RM per exercise, name-keyed), **history**; **seeded** with the 4-day Upper/Lower program as startable routines (the researched training content lives on). **Together** — a shared weekly challenge (household count + per-person days) + a combined recent-activity feed. Local-first (`hb-workouts-{p}` / `hb-routines-{p}`; one in-progress session at a time, `done` flag). **Searchable exercise library `src/lib/exerciseData.ts` = 186 exercises** (8-agent Workflow → `scripts/exercisedata/` → `merge.mjs`; lazy ~2.7 kB-gz). Search by name/muscle/equipment, or add a custom exercise.
> - **Retired (git-preserved):** the static per-person diet/training PROSE — `GinoPlan`/`XinyanPlan`/`MacroNeeds`/`PersonalWorkouts` + the `Card`/`SecHead`/`Macros`/`Pillars`/`Session` helpers + `TRAINING`/`MESO` data. The training program survives as **seed routines**, the calibration gauge as `CalibrationGauge.tsx`. Bilingual (~55 ZH SEED strings added, no dup keys; muscles/equipment translated). Numeric inputs string-buffered + clamped (`NumField` / `NumIn`). **Verified end-to-end via `?meallab`** (now hosts BOTH sections via a Meal/Workout toggle).
> - **NOT yet:** workout + meal day-logs are **local-only** (no Supabase cross-phone sync — same follow-up as the meal logs; a 3-lens review flagged that Solo/Together cache the day log independently, so concurrent multi-tab/second-device edits to the same key can last-writer-win — low severity, the proper fix is the shared-store/sync work, NOT a naive storage-reload which could clobber an in-progress edit); macro target = static `DAILY` (the calibration gauge is advisory, doesn't yet change the target); `solveMeal`/`scaleTarget`/`ROLE_*` in `nutrition.ts` are still dead exports.
>
> **⭐⭐ 2026-06-20 — HEALTH MEAL BUILDER FULLY REBUILT (shipped live, commit `f9b89ea`; deploy green).** The finance "counting + updating stack" ported to Health. The old two-person portion *solver* is GONE — replaced by a **real-time macro BURNDOWN**: daily target (`DAILY[person]`) = the budget, each logged food = a spend, the gradient hero counts down **"kcal left to eat today"** with Protein/Carbs/Fat bento tiles burning down live + a **next-meal allowance** = remaining ÷ meals-still-ahead (recomputes the moment a meal gets food). Two modes (`hb-meal-mode`): **Solo** (one person's day) and **Together** (build ONE shared meal with per-person grams → a **double breakdown**: two columns each drawing down that person's OWN daily remaining; **"Log this meal for both"** writes each portion to that person's day log).
> - **Engine:** `src/lib/mealLog.ts` — pure macro math (`contribution`/`dayTotals`/`remaining`/`nextMealAllowance`/`mealsAhead`), local-first day logs (`hb-meallog-{person}-{YYYY-MM-DD}`, **local** date via `todayStr`), `searchFoods` (token-AND ranking + barcode match). `Macros` type; `LoggedItem` snapshots per-100g so the log survives later library edits.
> - **Hidden food library:** `src/lib/foodData.ts` — **878 USDA-standard foods** (incl. Chinese/Asian), generated by a 12-agent Workflow → `scripts/fooddata/raw/*.json` → `scripts/fooddata/merge.mjs` (validates `4p+4c+9f≈kcal`, dedupes, `fdc-*` slug ids; re-runnable). **Lazy-imported** (`import("./foodData")`, own ~21 kB-gz chunk) so finance mode stays lean. UX: **search by name / scan / type a barcode — never a long list**; custom-food manual entry preserved (`CustomFoodForm` → `addFood`).
> - **UI:** `src/views/MealBuilder.tsx` (full bento: gradient hero, rounded-[18px] tiles, `BRAND_GRADIENT`, search/portion/dual overlay sheets, `NumField`). `HealthView` passes `owner`+`person`. **Bilingual** — ~45 ZH strings added to `i18n_zh.ts` SEED (no dup keys; `kcal`→大卡 to match AUTO_ZH; macro "over" reuses finance's `over`=超支, on-theme for the budget metaphor).
> - **Robustness:** grams inputs are **string-buffered + clamped [0,5000]** (`NumField`) so a fat-fingered paste can't poison the local log — the ONLY finding from a 3-lens adversarial review (engine-math + integration/i18n lenses came back clean), now fixed + verified. **Verified live end-to-end** via a DEV-only **`?meallab`** harness (tree-shakes from prod like `?lab`): solo burndown + allowance recompute, search→portion→add, Together dual-write to both day logs, the clamp, and 中文.
> - **NOT yet (next steps):** day logs are **local-only** — no Supabase sync across the two phones yet (mirror the `foods` table pattern). Food NAMES stay English (chrome is translated). Macro target = the static `DAILY` map (not yet wired to the calibration gauge / weekly scale). `solveMeal`/`scaleTarget`/`ROLE_LABEL`/`ROLE_ORDER` in `nutrition.ts` are now dead exports (harmless; sweepable).
>
> **⭐⭐ 2026-06-19 — FINANCE MODE WAS FULLY RESKINNED + RESTRUCTURED (shipped as the DEFAULT, live). Much of the "Mission Control one-pager" language below is now HISTORICAL.**
> Finance mode is now a bright **4-tab bento app: Home · Insights · Activity · Profile**, in `src/views/redesign/`:
> - `FinanceTabs.tsx` — the container (`useStore` → builds view-models via `buildVMs.ts`; a working Finance/Health + Mine/Household top bar; renders the 4 tabs + all the sheets).
> - Presentational tabs `HomeTab` / `InsightsTab` / `ActivityTab` / `ProfileTab`; bento sheets `BillsSheet` / `CategorySheet` / `TxnSheet`; palette `lib/catColor.ts` (`catColor`/`catIcon`/`BRAND_GRADIENT`/`conicFromSegments`).
> - `App.tsx FinanceGate` renders `FinanceTabs` by **default**; **`?oldui`** falls back to the original `OnePager` (kept ONLY because the new UI imports its exported sheets: SprintSheet/MarkSentSheet/PayBillSheet/AccountsSheet/SettingsSheet; the standalone LedgerSheet/AddTransactionSheet/ImportSheet are reused too). `?lab` = the no-login design lab (`DesignLab.tsx`, mock data) for verifying the new UI without auth.
> - **Bills** now live on the Home tab (a tile → `BillsSheet`: upcoming list + "X paid" + a tappable money calendar; tap a bill → `PayBillSheet`).
> - Budget = **7 lines** now (added a **Pets** category): Groceries500/Gas250/Dining150/Household90/Health110/Pets100/Subs50 = $1,250.
> - **✅ FUZZY BILL-MATCHING FIXED & SHIPPED (2026-06-19, commit `367c3fd`; edge fn redeployed):** bank bill-payments now auto-flip the correct bill. `categorize.ts` (both copies) gained `billKey()` + `matchRecurringName()` — a normalized name resolver (exact → punctuation/case/ellipsis-folded → `merchantKey`). The Plaid edge fn (`plaid/index.ts`) resolves bill payments via it, then a **conservative** day+amount fallback (`matchBillByDayAmount`: ±3 days, amount within $15/15%, **unambiguous-only** — returns null rather than risk attributing to the wrong bill). `importStatement.ts` uses the same resolver for the manual importer. Frontend `recordedBill` (in `buildVMs.ts` + `OnePager.tsx`) now snaps a recorded payment to the recurring's **nearest scheduled installment**, so a feed row logged on the raw post-day (when a recurring row has no baked `due_days` and `monthlySchedule` falls back to the legacy `DUE_DAYS` map) still flips the right bill; multi-installment bills (Mom 15/30) still map each payment to its own installment. Logic unit-tested (25 cases).
> - **✅ 2026-06-19/20 FIX BATCH (all live, commits `5964fa8` + `e95d8cf` + `acf2949`):** (a) **iPhone safe-area** — the bento `FinanceTabs` TopBar now applies `paddingTop: calc(env(safe-area-inset-top)+10px)` (HealthView + bottom TabNav already had it) → header clears the Dynamic Island, and the top-right Mine/All toggle is tappable again. (b) **Profile preferences were dead** — the `Segmented` (Language EN/中文, Default view Mine/Household) + variable-bill `Toggle` were display-only; now wired (`onLang`→`useLang().setLang`, `onLens`, `onToggleVariableBill`→`setRecurringVariable`). (c) **"Still processing" indicator** — `schema_v13_pending.sql` adds `accounts.pending_hold`; edge fn `pickHold()` = `max(0, current−available)` for depository (0 for cards), written per-account after the atomic balance sync; frontend `Account.pendingHold` + `totalPendingHold()` + `HomeVM.processing` (lens-aware) → Home cash tile shows "~$X settling" in amber. Safe-by-design: cash stays = available (already net of the hold), no pending ROWS are inserted, so a charge posts ONCE via the deduped feed and the hold auto-shrinks (no duplicate / double-withdraw). BofA doesn't itemize pending → aggregate hold is the only signal it gives.
> - **⚠️ DEPLOY-VERIFY LESSON:** the CI build runs `tsc -b && vite build`; **local `tsc --noEmit` gave a FALSE GREEN** (stale incremental cache) and 3 Pages deploys silently FAILED on a real type error while I thought they shipped. ALWAYS verify with **`npm run build`** (PATH-refresh first) before claiming a frontend change is live, and `gh run watch <id> --exit-status` to confirm the deploy.
> - **⭐ Gino must RUN `supabase/schema_v13_pending.sql`** (one idempotent `ALTER TABLE accounts ADD COLUMN pending_hold`) in the Supabase SQL editor for the processing indicator to populate (edge fn write no-ops safely until the column exists; frontend reads 0).
> - **✅ 2026-06-20 BATCH 2 (all live, commit `631c6ae`; edge fn redeployed; schema_v14 applied + debts armed via Management API):**
>   - **Cash account LIST now lens-correct** — `AccountsSheet` takes an optional `accounts` prop; `FinanceTabs` passes own+joint in "Mine"/all in "Household" (matching the tile total); `buildVMs` profile accounts same. (The total was already lens-aware; only the list was wrong.)
>   - **Active tab persists** (`hb-fin-tab`) so the LanguageProvider key-remount on language switch no longer bounces you to Home.
>   - **DEBT AUTO-TRACKING (issue: "auto-update the debt plan from payments"):** (1) **Double-count guard** — `applyMoneyEvent` NO-OPs when the resolved debt is auto-tracked (`providerAccountId || trackPattern`); this closed a LIVE double-count where MarkSent/PayBill decremented a bank-linked card on top of the trigger + double-debited cash. `MarkSentSheet` (new `autoTracked` prop) + `PayBillSheet` (new `feedOwned` prop) now show a "the bank records this automatically" reminder instead of a no-op write with false success. (2) **Non-bank debts feed-tracked** — `schema_v14_debt_tracking.sql` adds `debts.track_pattern / tracked_baseline / tracked_since` (+ CHECK that a pattern is armed only with a fixed baseline+since). Edge fn `plaid/index.ts`: a feed OUTFLOW whose description contains a tracked debt's `track_pattern` is recorded `appliesTo={kind:debt,settled}` (precedence over classify), then each tracked debt is recomputed **balance = tracked_baseline − sum(payments since tracked_since)** SET every sync (idempotent, reversible — **verified NO DRIFT across 2 live syncs**). Only fires when `track_pattern AND tracked_baseline` set AND `provider_account_id` null (cards stay owned by the v12 trigger). (3) **Live arming:** the two Affirm loans MERGED → one **"Affirm" $288.78** armed on `AFFIRM`; **Mom (China) $800** armed on `Remitly` (NOT "REMITLY" — Plaid's clean merchant_name is "Remitly"; Pandaremit excluded); the $200 Affirm recurring bill RETIRED (`active=false`; plan-math-safe — `DEBT_PAYMENT_RX` already netted it out of firepower). Total debt unchanged $5,836.65.
>   - **Adversarially reviewed (4-lens Workflow) BEFORE deploy** — caught + fixed 2 criticals (baseline `?? d.balance` fallback that would walk a debt to 0; "REMITLY"→"RMTLY"/"Remitly" descriptor) + the PayBillSheet no-op + dead `/AFFIRM/i` bill rule.
> - **⭐ NEW CAPABILITY: I can run DDL/SQL myself via the Supabase Management API** — `POST https://api.supabase.com/v1/projects/ganzefaciiyibselizqi/database/query` with `Authorization: Bearer <SUPABASE_ACCESS_TOKEN>` body `{query}`. schema_v14 + the debt arming were applied this way. **No more "Gino runs the SQL editor" chores** — apply schema for him.
> - **✅ (2b) BENTO i18n DONE & LIVE (commit `68463ca`):** every user-visible string across the 9 bento files + `buildVMs` wrapped in `t()`; ~90 ZH SEED entries added to `i18n_zh.ts`. Verified in `?lab` under 中文 (Home/Insights/Activity/Profile + Bills calendar all render Chinese, weekday row `日一二三四五六` via a single `"S M T W T F S"` key). The language toggle now actually translates.
> - **✅ DEAD-CODE + DEAD-PIPELINE SWEEP DONE & LIVE (commit `8d3ca8a`, −401 net lines; edge fn redeployed; verified no drift):** 5-agent read-only discovery (54 findings) → applied the build-verified SAFE removals only. Removed: dead exports/helpers (ColorPicker/EmojiPicker/SWATCHES/GOAL_EMOJIS in `ui.tsx`; `phoneIcon`/`cardIcon`; `useActiveSection`; `ownerLabel`/`ownTransactions`; `clearOwner`; the dead `Tab` union; `.hide-scroll`); Vite scaffold orphans (`src/App.css`, `src/assets/{react,vite}.svg`+`hero.png`, `public/icons.svg`); the **`pending_preview` write/delete pipeline** in the Plaid edge fn (wrote every sync, NOTHING read it — `pending_hold` replaced it) + the unused `ownerByProv`/`consecutive_failures`/`NormalRow.plaidCategory`. **PII fix:** the `MOCK_INSIGHTS/ACTIVITY/PROFILE` fixtures (real email/bank/last-4s) were shipping in the public JS bundle → moved into the **dev-only DesignLab** (sanitized) + made `?lab` **lazy + `import.meta.env.DEV`-gated** so the harness AND mocks tree-shake out of prod (verified: 0 occurrences of email/MOCK_/DesignLab in `dist/`; `?lab` still works in the dev preview).
> - **✅ (1) `?oldui` OnePager RETIRED & LIVE (commit `ea954ba`, −1,800 net lines; deploy green):** `OnePager.tsx` (2,398 lines) → `src/views/sheets.tsx` (811 lines = ONLY the 5 reused sheets `AccountsSheet`/`SprintSheet`/`MarkSentSheet`/`PayBillSheet`/`SettingsSheet` + their private helpers `Eyebrow`/`PaymentSheet`/`ConnectBank`/`CreditCardLinks`/`CreditCardRow`/`shortDebt`/`fmtDay`). The OnePager component + ~13 body-only components + 9 orphaned helpers + ~50 dead imports were deleted by line-range; the compiler (`noUnusedLocals`) drove the cascade — all "unused", never "cannot find name", so nothing live was cut. `FinanceTabs` imports from `"../sheets"`; `App.tsx` lost the OnePager import + the whole `?oldui` `FinanceGate` branch (bento is the ONLY finance UI now). Also deleted orphaned-by-removal: `src/lib/hooks.ts` + `src/components/TransactionRow.tsx`; removed OnePager-only CSS (`.hero-bar/.slip-recess/.reveal/.is-visible/.breathe/.ring-pulse`+`--color-steel`; kept `.pop`+`.rise`). **3-lens adversarial audit (integrity/wiring/dangling) came back CLEAN.** ⚠️ `?oldui` no longer exists — there is NO graphite fallback; the bento is it. **⭐ STILL FLAGGED (NOT done):** **(2) Goals subsystem** (`SavingsGoal`/`data.goals`/`addGoal`/`contributeGoal`) — loaded+persisted but NEVER read by any view; remove if confirmed unwanted. **(3) Dead store methods** `addDebt`/`payDebt`/`setPaidBill` (zero callers; live paths are card-linking / `payDebtExtra` / `markBillPaid`). **(4)** DDL drops (the `pending_preview` table, `consecutive_failures` column) — I can apply via the Management API. **(5)** Minor dup: `ProfileTab` local `Segmented` vs `ui.tsx` `Segmented`; `ActivityTab` inline money fmt vs `lib/format`; the `buildVMs`↔OnePager assembly dup (dies with `?oldui`). **(6)** Stale docs `docs/STATUS.md` + `docs/debt-attack-plan.html`; archive `_homebase_backup_*.json` off the project dir. **(7)** Carried: repaint reused OnePager sheet *interiors* bento; feed-income recording; Sam's-Club split. If Gino sends family support via **Remitly** (not just the China debt), revisit "every Remitly = the debt".
> - **✅ 2026-06-20 UI/PLAN POLISH (from Gino's live testing; all live):**
>   - **`9fbfa1a`:** Cash tile "settling" → **"~$X waiting to post"**. **Streak tile REMOVED** (it opened the same `SprintSheet` ladder as Debt left) → **Budget is now a full-width container** (donut + spend-vs-target bar + "$X left"). The **`SprintSheet` attack ladder is READ-ONLY** — removed the per-debt "Make a payment" button + the whole `PaymentSheet` (debts auto-update from the feed now). **Activity filter chips now actually filter** (All / In budget=envelope / Needs review=review — the `filter` state was set but never applied to `vm.rows`) + an empty state. **Refresh** shows spinner + "Refreshing…" then "Updated ✓".
>   - **`d2bd300` — EMPTY-CONTAINER FIX:** counts and the lists they open were filtered DIFFERENTLY — `sinceMonday`/`anomalyIds` came from `data.transactions` (unfiltered) but the lists (`visible`/`ledgerTxns`) are lens-filtered, so in **Mine** a count could be non-zero (counting the spouse's txns) while the list opened EMPTY. Now `sinceMonday` + the anomaly read from the lens-filtered `visible`. Also the Activity tab was capped at 30 rows while `needsReview` counted all 47 across 199 → now the tab is **THIS-MONTH (matches the header) + UNCAPPED** so the badge equals the filtered list. (Measured live: Mine sinceMonday $199 vs household $554; the lone anomaly was Xinyan's Sam's Club.)
>   - **`08c03d3` — FIREPOWER NOW REFLECTS OVERSPEND (core plan change):** `firepower` was plan-based (`income − fixed − the $1,250 budget TARGET`), so overspending didn't reduce what the app told you to send at debt. Now in `buildVMs`: `overspend = max(0, variableSpentThisMonth − target)`, `firepower = max(0, math.firepower − overspend)` — ASYMMETRIC (overspend reduces it live; **under-spend does NOT inflate** it — mid-month "under" is just the month not over yet). Fed to `payoffSchedule` so the hero number, the "Next move" amount, AND the debt-free date all reflect real available cash. Hero shows a **"−$X · over budget this month"** chip when active. `payoffSchedule` is safe at low firepower (`≤0` → empty, 240-payday guard). `math.firepower` is still the raw plan; `firepower` (the const) is the effective one — use it. ⚠️ The Insights "At debt" + Home hero now use the EFFECTIVE firepower, not `math.firepower`.
> - Categories are **code-driven** (`src/lib/seed.ts DEFAULT_CATEGORIES`, not a DB table). Verification gotcha: `preview_screenshot`/async `preview_eval` **time out** (Vite HMR socket blocks network-idle) — use **synchronous** `preview_eval` DOM reads, and split click→read across separate eval calls so React flushes. The authed app can't be lab-verified (no login in preview) → typecheck+build guard those.

---

## What it is
A personal finance **+ health** PWA for **Gino** and his wife **Xinyan**, used daily on their phones. One app, **two modes** toggled at the top:
- **Finance** — a debt-payoff "Mission Control" one-pager.
- **Health** — two-person diet/training plans + a calibration gauge + a macro meal builder.

The unifying idea: the app is a **calibration instrument** — Finance calibrates money off the ledger, Health calibrates two bodies off the weekly scale. "Measure, don't infer."

## Live + repo + deploy
- **Live:** https://voeximus.github.io/homebase/ (GitHub Pages, installable PWA).
- **Repo:** github.com/Voeximus/homebase (authed `gh` account **Voeximus**).
- **Deploy:** push to `main` → GitHub Actions builds + deploys (~60–90s). `.github/workflows/deploy.yml`. Build secrets `VITE_SUPABASE_URL` + `VITE_SUPABASE_PUBLISHABLE_KEY` are set in the repo.
- Local path: `C:\Users\ginoc\Documents\homebase`.

## ⭐ Live bank feed (Plaid) — 2026-06-19
Homebase pulls **real Bank of America** balances + transactions automatically (read-only). **Bank = truth:** a connected account's balance is overwritten by the bank's number each sync; transactions are history + budget fuel, not balance-movers — so drift is impossible. Provider = **Plaid, "lean" mode** (cron/on-demand sync, no public webhook), chosen over SimpleFIN/Teller after deep research (free Trial plan ≤10 bank logins, true OAuth, native pending→posted, paved React path).

**Backend (Supabase `ganzefaciiyibselizqi`):**
- `supabase/schema_v8_bankfeed.sql` (RUN) — `bank_connections`, `pending_preview`, transactions provenance cols (`provider`, `provider_txn_id`, `status`, `needs_review`) + partial unique index, accounts cols (`connection_id`, `provider_account_id`, `balance_synced_at`), and `get_connection_token()` — a SECURITY DEFINER **service-role-only** reader of the bank token from **Supabase Vault** (token never reaches the client).
- `supabase/schema_v9_bankfeed_rpc.sql` (RUN) — `store_connection()` (stash token in Vault + open a connection) + `apply_bank_sync()` (atomically upsert posted rows, idempotent on provider_txn_id, + set the account's bank-truth balance). Service-role-only.
- **Edge Function `supabase/functions/plaid/index.ts`** — one multiplexed function: actions `link_token` / `exchange` / `sync` / `disconnect`. Holds the Plaid secret + bank token server-side ONLY; JWT-verified. Imports `_shared/plaidSync.ts` (the `reconcile()` pending→posted engine) + `_shared/categorize.ts` + `_shared/categorizeData.ts` (the full trained categorizer, **copied** from `src/lib` — keep in sync). `pickBalance()`: depository → **available** (spendable), credit → **current** (owed). Only "variable" living-spend is inserted; income/transfers/bills skipped.
- **Deploy:** `npx supabase functions deploy plaid --project-ref ganzefaciiyibselizqi` with `SUPABASE_ACCESS_TOKEN` env (a personal token "Hombase", revocable in Supabase → Account → Access Tokens). Secrets (`PLAID_CLIENT_ID`, `PLAID_SECRET`, `PLAID_ENV=production`, `PLAID_REDIRECT_URI`) are in **Supabase function secrets — NOT the repo.** Docker not needed (CLI bundles via API).

**Frontend:**
- **Connect a bank** in `SettingsSheet` (OnePager) via `react-plaid-link`. `lib/plaidClient.ts` + `components/PlaidOAuthReturn.tsx` (mounted in `App.tsx Shell`; resumes Link after a bank's OAuth redirect — real banks need OAuth + a `redirect_uri` registered in Plaid → Developers → API → Allowed redirect URIs = `https://voeximus.github.io/homebase/`).
- **The Ledger** — `components/LedgerSheet.tsx`, reached via **Activity → "All activity."** Every transaction with its budget fate (→ envelope / "Bill · not in budget" / amber "Needs review"), search + All/In-budget/Needs-review filters, grouped by month. **One-tap re-categorize that TEACHES** (`setTransactionCategory` + `saveMerchantRule`) = the categorizer-training bench, plus the merchant-policy controls below.
- **`totalBalance` excludes credit accounts** (`lib/recurring.ts`) so a card never inflates cash. New store actions: `excludeFromBudget` (mark a one-off → out of variable spend) + `makeRecurringBill(txnId, cadence)` (promote a sub to a bill).

**Merchant = policy (the categorizer vision, locked with Gino):** a merchant isn't one category, it's a policy — **Fixed** (Safeway→groceries), **Ask** (gas/Sam's: per-purchase), **Recurring** (sub→bill). Built: the recategorize sheet's **"✓ Remember merchant / Just this one"** toggle (Ask merchants — a gas-station snack → **Dining** without making all gas dining), and **"Repeats monthly/yearly?"** → `makeRecurringBill` (sub becomes a calendar bill, leaves variable spend, teaches a bill rule). **NOT yet built:** the **Sam's-Club split** (one charge → $X groceries + $Y household, by dollar — **NEXT**) and the **card→debt wiring** (below).

**⭐ CURRENT LIVE STATE (post-wipe, Plaid is truth):** Gino's BofA connected (status ok). Accounts: **Adv Plus Banking $1,306.67** (Gino's checking, the old "Geo"), **Adv SafeBalance $15.48** (the **JOINT** — re-tagged owner Joint; available balance), **BankAmericard $4,156.78** (the **…4728 card**). **57** plaid transactions. Debts (5) / recurring (14) / merchant_rules (6) intact. The **…4728 card is a credit-typed account EXCLUDED from cash** — but **NOT yet wired to the …4728 debt**; the debt is still the manual $4,156.78 row. Categorizer audit found ~21/57 needing attention (Remitly remittances + card interest in "other"; Chipotle/Canes/7-Eleven/itch.io dict misses) → Gino triages in the Ledger.

**Decisions locked this session:** card spending **tracked** (max fidelity; Gino won't use …4728 going forward); China-trip one-offs (Remitly to Xinyan, SWA/passport fees) **skipped**; gas snacks → **Dining**; subscriptions → **bills**; splits **by dollar**.

**⚠️ The wipe (2026-06-19):** old manual accounts + transactions + sandbox test data cleared so BofA could connect into a clean slate; **debts + recurring + merchant_rules + goals KEPT.** Full pre-wipe backup at `_homebase_backup_2026-06-19T*.json` (repo root, **gitignored**, restorable).

**⭐ Auto-sync (2026-06-19):** the feed syncs **on every app open** (App.tsx `syncNow()`) + a **Refresh** button in the Activity header (`force=true` → `/transactions/refresh` nudge, then sync). No background cron yet (offered). Honest ceiling: Plaid pulls ~1–4×/day + the bank's own pending lag → **same-day loop, not minutes-after-swipe.** `lib/plaidClient.ts syncNow(force)`.

**⭐ Bills precision (2026-06-19) — variable bills + the feed records bill payments:**
- `recurring.variable` flag (**schema_v10, RUN**). A variable bill projects from `billExpected(bill, txns)` (`lib/plan.ts`) = **rolling average of its last 3 actual `appliesTo=bill` payments** (fallback to modeled amount); fixed bills keep the modeled amount.
- **Injected DISPLAY-ONLY** — `monthlySchedule(recurring, monthKey, transactions)` (`schedule.ts`) uses billExpected for variable out-bills → calendar/chips/upcoming show the estimate; **householdMonthly / planMath / payoffSchedule stay on contracted amounts**, so firepower + the countdown are insulated (load-bearing safety rule).
- **The feed now RECORDS bill payments** (skipped them before): a posted bill-classified txn is matched to its recurring (`billName`→name; post-day snapped to nearest `due_days`), written `appliesTo={kind:bill,recurringId,monthKey,day,settled:true}` with the real amount → **auto-marks paid + logs the actual.** Idempotent (provider_txn_id unique) + dedups vs manual (`paidBill` set). **schema_v11 (RUN)** makes `apply_bank_sync` carry `applies_to`. New `setRecurringVariable` action.
- **Electric (SRP) + Verizon** are the only variable bills, flagged + **seeded** with Mar–May actuals from `MasterLedger.csv` → project **~$89.92 / ~$82.83** now. Every bill carries `due_days`, so the feed↔calendar day-match is solid (**verified**).
- ✅ **Pay-early auto-mark-paid VERIFIED:** pay a bill early (same month) → feed sees it → snaps to due day → marks paid + logs actual + re-averages. Caveats: bank/Plaid latency (hours); paying a *full month* ahead marks the wrong month.

**PENDING (bank feed):**
1. **⭐ 3 bills-precision polish items (NEXT — Gino wants ALL 3):** (a) a **toggle UI** to flag a bill variable (now set via script; extend PayBillSheet or add a small BillEditSheet from the Bills container — `setRecurringVariable` exists); (b) an **"~est" badge** on variable chips (ScheduleEntry carries `variable`); (c) an **editable amount at pay-time** for variable bills (PayBillSheet, OnePager ~1883-1918).
2. **Sam's-Club split** (one charge → $X groceries + $Y household, by dollar) — waits on Li's card to test.
3. **Card → …4728 debt auto-update** — DESIGNED (`debts.provider_account_id` + `link_debt_to_provider`/`set_debt_balance` RPCs; route credit accounts to the debt) but **NOT built/run** (schema_v12). Card excluded from cash; debt is manual $4,156.78.
4. **Li (Xinyan) connects HER BofA** on her phone.
5. **Yearly** sub→bill calendar rendering unverified (monthly solid).
6. **🔒 Confirm the test-account backdoor was DELETED** in Supabase Auth (exposed in this PUBLIC repo). Maintenance now uses the **service-role key** via the "Hombase" access token (`npx supabase projects api-keys`).

## Stack + dev workflow
- Vite + React 19 + TypeScript + Tailwind v4 + lucide-react. Supabase (Postgres + auth + realtime). vite-plugin-pwa. @zxing/browser (barcode, lazy). pdfjs-dist (PDF import, lazy).
- **Run dev server:** `preview_start` name **`homebase`** (config `C:\.claude\launch.json`, port 5173). The **serverId rotates every session and dies between sessions** — call `preview_start` to get a fresh one; if screenshots start timing out (the realtime websocket blocks network-idle on a long-lived page), **stop + start a fresh server** for a clean screenshot window. Mobile viewport: `preview_resize` 390×844.
- **Build/typecheck:** `npm run build`. **PATH refresh required first** in PowerShell:
  `$env:Path = [System.Environment]::GetEnvironmentVariable('Path','Machine') + ';' + [System.Environment]::GetEnvironmentVariable('Path','User')`
- **Commit messages:** write to a temp file and `git commit -F file` (heredoc/inline quoting breaks on the `"`/`'` in messages). End with the Co-Authored-By trailer.
- **preview_click does NOT reach React** in this env (synthetic events). To click in-app, use `preview_eval` with a real DOM `.click()`. Reading state: `preview_eval` / `preview_snapshot` / `preview_inspect` (reliable); screenshots work on a fresh server but flake on a stale one.

## Architecture
Provider tree: `App → AuthProvider → AuthGate → FinanceProvider → Shell → LanguageProvider → (OnePager | HealthView)`.
- **AuthProvider** (`auth/AuthProvider.tsx`) — Supabase email/password session.
- **FinanceProvider** (`store/FinanceStore.tsx`) — THE data store: loads all tables, realtime-subscribes, and exposes the **pipelined money engine** (`applyMoneyEvent` moves cash + writes a ledger row + fans out to a linked debt/goal) plus actions: `payBill`, `payDebtExtra`, `markBillPaid`, `commitImport`, `setAccountBalance`, `setTransactionCategory`, `excludeFromBudget`, `makeRecurringBill`, `deleteTransaction`, `saveMerchantRule`, `addFood`/`deleteFood`, `seedHousehold`, `resetAll`.
- **Shell** (`App.tsx`) — holds `mode` ('finance'|'health', `hb-mode`), `owner` ('gino'|'xinyan', `hb-owner`), `lens` ('me'|'all', `hb-lens`), and in-memory `entered`. While not `entered` it renders `WelcomeScreen` (the front door, shown every cold launch); after, OnePager / HealthView with owner + lens props.
- **LanguageProvider** (`components/LanguageProvider.tsx`) — holds `lang` ('en'|'zh', `hb-lang`); **remounts its children via `key={lang}`** so every module-level `t()` re-evaluates on toggle (no data refetch since it's below FinanceProvider).

## Features + key files
**Finance — `views/OnePager.tsx`** (one scrolling page; sticky collapsing header + jump-chip scroll-spy):
- Hero "next move" (the VERB — what to send, when, split across debts, from `payoffSchedule`); "Mark sent" → confirm sheet loops `payDebtExtra`.
- Cash + 90-day streak ring · debt "runner road" to the Nov '26 finish · firepower + spent · 6-envelope budget (drill→relabel/delete) · bills chips + collapsed calendar + coming-up · activity + log/import + floating ＋. SettingsSheet (seed/import/clear) lives at the bottom of this file.
- Math/data: `lib/plan.ts` (planMath, orderedDebts, payoffSchedule, commitmentProgress, variableSpentThisMonth, spentByCategory, lineSpent, LEAN_VARIABLE, UPCOMING_INCOME, ONE_TIMES, HABITS, SAVINGS_SPLIT), `lib/schedule.ts`, `lib/recurring.ts`, `lib/household.ts` (SEED_*), `lib/format.ts`.
- Import/categorizer: `lib/{importStatement,importPdf,categorize,categorizeData}.ts` + `components/ImportSheet.tsx` (CSV+PDF, trained on his MasterLedger, dedup, NPC clarify-cards → `merchant_rules`).

**Health — `views/HealthView.tsx`** (sub-toggle `hb-health-sub` = 'plan'|'kitchen'):
- PLAN: per-person Gino(BUILD)/Xinyan(CUT) toggle (`hb-health-person`), accents Gino `#ef8136` / Xinyan `#2dd1c0`. Snapshot, daily-fuel macros, training (4 sessions + 5-week table), why-it-works, the calibration gauge (enter 7-day weight + weeks → rate → verdict; Xinyan also progress-to-118). Inputs persist per-person in localStorage.
- KITCHEN: **`views/MealBuilder.tsx`** + **`lib/nutrition.ts`** — pick ingredients → per-person grams. `solveMeal(foods, target)` fills veg→carbs→protein→fat to each person's meal target (settable ⅓/½/full-day) so one plate yields two portions; flags overshoots. ~24 seeded foods + custom foods.
- Barcode: **`components/BarcodeScanner.tsx`** (back camera via @zxing, lazy) + **`lib/barcode.ts`** (OpenFoodFacts lookup, free/no-key) → prefill the Add-Food form. Camera needs HTTPS (works on the live site; untested in preview = no camera).

**i18n (English / 简体中文)** — `lib/i18n.ts` (`t(s, vars?)`, module-level current lang) + `lib/i18n_zh.ts` (hand-curated SEED) + `lib/i18n_zh_auto.ts` (~464 workflow-generated translations; SEED overrides on conflict). Toggle = `LangToggle` (中/EN) in both headers. ~470 strings wrapped across the views/components; interpolated sentences use `{placeholder}` templates so word order is correct; money/dates/names/dynamic-data stay raw; untranslated → English fallback.

**PWA / mobile** — `vite.config.ts` (manifest, theme `#0a0d12`, icons incl. 1024px), `index.html` (viewport-fit=cover, theme-color), `public/favicon.svg` (graphite house, cyan→green) + generated icons, `pwa-assets.config.ts` (`@vite-pwa/assets-generator` devDep, dark bg / no white padding). Touch polish + safe-areas in `index.css` (the instrument theme `@theme` tokens live here too).

**Welcome + owner lens (NEW 2026-06-19)** — `components/WelcomeScreen.tsx` is the front gate (gated in `Shell` via in-memory `entered`, so it shows every cold launch). First launch → "whose phone is this?" → "you're {name}?" confirm → binds the device (`lib/owner.ts`, `hb-owner`); thereafter it greets by name + shows the **throw-rail** (a filled two-segment Finance/Health switch) → throw to enter. The owner is then a **lens** (`lib/lens.ts` filters + persisted `hb-lens`, default `me`; `components/LensToggle.tsx` Mine/Household in each header): **Finance · Mine** = your accounts' cash (Joint as a small line) + your activity, with the shared next-move + sprint on top and budget/bills collapsed (also off the jump chips); **Health · Mine** = locks to the owner and hides the person toggle. "Household" restores the full picture in both. The household math is unchanged — the lens only changes display. Owner-change-in-Settings: offered, NOT built (today: clear `hb-owner`).

## Data model + Supabase
- Project `ganzefaciiyibselizqi`, `https://ganzefaciiyibselizqi.supabase.co`. Auth email+password; **RLS = authenticated-full (shared household)**. Tables: accounts, recurring, transactions, debts, savings_goals, paid_bills, merchant_rules, **foods** (v6). DDL in `supabase/schema*.sql`.
- **Maintenance access** — credentials live in the **private agent memory only**, NEVER in this public file. Prefer the **service-role key** (fetched via the Supabase CLI with the access token) for live-data scripts; the legacy test-account backdoor is being retired (its creds were exposed in this public repo's history → delete that user + rotate). The publishable key `sb_publishable_907KbW_QmcTvL-wFHg-8yA_roZe8u_2` is the anon key — safe in the public bundle.

### Reconciled financial baseline — HISTORICAL (pre-Plaid; superseded by the live feed)
_The hand-reconciled manual data, **WIPED 2026-06-19** when the Plaid feed went live (full backup kept — see "Live bank feed"). Kept only for reference; live numbers now come from BofA._
Accounts: **Geo $1,566.26 · Xinyan $751.00 · Joint $18.74 = cash $2,336.00**. Debts: **Anthropic $99.10 · Amazon $189.68 · …6813 $591.09 · Mom-China $800 · …4728 $4,156.78 @26.49% = $5,836.65** (pre-test every debt is AT its original_balance — no prior payments recorded).

### ⚠️ NO SANDBOX (load-bearing)
Every tap in the deployed/preview app writes to the **live Supabase DB**. On 2026-06-18 Gino feature-tested thinking it was a sandbox → 7 stray `payDebtExtra` rows drove cash to −$266; restored exactly via a reversal script. **Health-mode food library + gauge + meal selection are local-first (localStorage) and do NOT touch Supabase** until `foods` syncs. A "practice mode" was offered, not built.

## OPEN ITEMS / pending Gino actions
1. ✅ **DONE (2026-06-18):** Gino ran `supabase/schema_v6.sql` — the `foods` table is live (RLS verified, reachable). **Food-library cloud sync is ON.** Each device's local foods migrate up on next open.
2. ✅ **DONE (2026-06-19):** new sign-ups are **disabled** in Supabase → Auth (verified by screenshot) — the open-signup hole the audit flagged is closed. RLS stays authenticated-full for the shared household; the test account still works.
3. To see the **new app icon** on a phone, remove + re-add the home-screen shortcut (PWA icons cache).
4. **Offered, not built:** owner-change in Settings (welcome owner currently changes only by clearing `hb-owner`); a "practice mode"/demo login (sandbox); logging the weekly scale over time (health trend).
6. **NEXT (Gino's plan):** redesign each mode's *layout* — how Finance and Health display info, now per **Mine vs Household** lens. The lens is the foundation; the layouts are the next pass.
5. **⚠️ NEVER re-seed** ("Clear all data" / re-seed wipes the reconciliation; merchant_rules + foods survive resetAll by design).

## Mode-layout redesign (2026-06-19) — lens-aware layouts
Each mode now has a distinct **Mine** vs **Household** layout:
- **Finance · Mine** — sticky header pins a 2-cell **vitals strip** (cash · debt); body = **upcoming bills** + your **activity**. No hero/sprint/budget.
- **Finance · Household** — a grid of **titled containers** (`ContainerCard`): Cash · Debt · Bills · Budget · Activity. `openContainer` state drills into one full-page (Debt = next-move hero + sprint + progress) with a `‹ All` back. Each section is gated `openContainer === "x"`; old jump-chips/`SECTIONS`/`useActiveSection` removed.
- **Health · Mine** — curated dashboard: `MacroNeeds` (from `lib/nutrition` `DAILY`) + a big **Scan a barcode** button (→ Meal Builder tab) + `PersonalWorkouts` (compact). Meal Builder is the other sub-tab.
- **Health · Household** — the full per-person plan (Gino/Xinyan toggle + `GinoPlan`/`XinyanPlan`), unchanged.
- Follow-up: the Health "Scan a barcode" button navigates to the Meal Builder tab; wiring it to auto-open the scanner (autoScan prop through MealBuilder→AddFoodSheet) is a refinement, not done.

## Backend audit (2026-06-19)
A 25-agent code audit + a read-only live-data check. **Live data is pristine** (cash $2,336.00, debts $5,836.65 all at original, 0 orphans, 0 non-reversible rows). Verified-correct: ledger math + exact reversibility (appliedAmount), the two earlier-fixed bugs, no-double-move on settled markers, lens-is-display-only, only the anon key in the bundle. Fixes shipped: SEED Xinyan 1095.75→751.00 (`89dd9cb`); **fail-closed money writes** — `applyMoneyEvent` now captures each balance-write error and `resyncLedger()`s to server truth so the UI can't keep a value that didn't persist (`5d69461`). **✅ `supabase/schema_v7_rpc.sql` RUN (Gino, 2026-06-19) + verified ($0 round-trip) + engine WIRED** — `FinanceStore.applyMoneyEvent` now calls `supabase.rpc("apply_money_event", …)` and `deleteTransaction` calls `reverse_money_event`, so the ledger row + cash/debt/goal move (or reverse) in ONE atomic transaction with in-row deltas (audit #2/#4 closed; `8edf108`). The old multi-step absolute-write path is gone; on any rpc error the client `resyncLedger()`s. Secondary direct-balance setters (`setAccountBalance`, `payDebt`, `contributeGoal`) still do absolute writes — lower-traffic, could move to deltas in a later pass. Import dedup is client-only (low risk: imported rows carry account_id:null, never double-move the anchored baseline) — a DB unique index is the durable fix.

## This session's commits (newest first)
**2026-06-19 — live bank feed:**
- `9f3a185` Categorizer policies: subscription→bill + 'just this one'
- `d775805` Full Ledger view + cash excludes credit cards
- `eebfd89` Plaid production OAuth (redirect handling)
- `a007299` Plaid bank-feed: connect button + edge function + sync + categorizer
**2026-06-19 — mode-layout redesign + audit:**
- `5d69461` audit: fail-closed money writes + atomic-RPC migration (pending run)
- `89dd9cb` audit: SEED Xinyan balance → reconciled 751.00
- `8820ddf` Meal builder: large one-tap Scan + 'meals per day' question
- `3a5d277` Health Personal — curated dashboard (macros · scan · workouts)
- `962f66d` Finance Full — drill-in titled containers
- `e1ab062` Finance Personal — lean view (cash/debt + upcoming bills + activity)
- `76342b0` Finance — lens toggle into header
- `0d19667` Finance Mine — pinned vitals strip
- `171c550` Per-person owner lens — Mine / Household (filter + collapse)
- `4e2235e` Welcome switch — filled two-segment pill
- `634d6f0` Welcome screen — per-device owner + throw-rail mode switch
- `4517f3c` Simplified Chinese (中文) toggle
- `ccd647b` Food library cloud sync (Supabase) + high-res app icons
- `5a689b7` Mobile-app treatment (install shell + touch polish)
- `5b4df65` Meal builder: barcode scan + OpenFoodFacts
- `28a484f` Health mode — two-body plans, calibration gauge, meal builder
- `6fa469a` Redesign: unified "Mission Control" one-pager + instrument theme
- (`9b21d6b` and earlier = the pre-redesign pipelined-ledger app)

## Working with Gino (style)
Director/game-designer; AI-paired build. WHAT is his, HOW is mine. One piece at a time, design-lens language, no yes-man, state confidence, measure-don't-infer, never manufacture scope panic. He test-drives live (his loop is test→insight→architecture). Reconcile/setup chores: **do them FOR him** via script, never hand him manual setup. Full collaboration detail in the memory files (`feedback_*`).
